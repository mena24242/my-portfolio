/* =====================================================================
   Supabase connection — the ONLY file you need to edit.
   Get both values from: Supabase Dashboard → Project Settings → API
   (the anon/public key is safe to expose in the browser; writes are
   protected by your dashboard password inside the database itself).
===================================================================== */
window.SUPABASE_URL = "https://agnwildabkhiynakumbd.supabase.co";        /* e.g. "https://abcdefghijk.supabase.co" */
window.SUPABASE_ANON_KEY = "sb_publishable_BMIU028OeN2lpFEX5RfivQ_zW_xIQM9";   /* e.g. "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...." */
