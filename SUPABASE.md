# 🗄️ خريطة البيانات على Supabase

آخر تحديث: **2026-09-19** — نسخة `setup.sql` v3 + **تتبع موقع الزائر** (المدينة والدولة من الـ IP — بدون GPS)

---

## المشكلة القديمة

كل محتوى الموقع كان متخزن في **خلية JSON واحدة ضخمة** جوّه جدول `portfolio`.
تفتح Supabase → Table Editor تلاقي سطر واحد مليون حرف — مش مقروء ومش قابل للتعديل.

## الشكل الجديد

المحتوى اتفكك لجداول حقيقية — كل جدول تشوفه وتعدّله مباشرة من
**Supabase Dashboard → Table Editor**:

| الجدول | فيه إيه | شكله |
|---|---|---|
| `site_content` | نصوص الأقسام: الهيرو، نبذة عني، تواصل معي، الفوتر، الإعدادات، التصميم | صف واحد لكل قسم باسم واضح |
| `site_projects` | المشاريع | صف لكل مشروع — أعمدة: العنوان، السنة، التصنيف، الدور، GitHub، الكود، النقاط |
| `site_experience` | الخبرات والأنشطة | صف لكل خبرة (العنوان، الدور، الفترة، التقييم، النقاط) |
| `site_education` | التعليم | صف لكل مرحلة دراسية |
| `site_services` | الخدمات | صف لكل خدمة (الأيقونة، العنوان، الوصف) |
| `site_skills` | مجموعات المهارات | صف لكل مجموعة — المهارات في عمود `chips[]` واضح |
| `site_certifications` | الشهادات | صف لكل شهادة (الكود، الجهة، الوصف، الرابط) |
| `site_testimonials` | التوصيات | صف لكل توصية |
| `site_meta` | وقت آخر حفظ | صف واحد |
| `portfolio_auth` | كلمة مرور الداشبورد (مشفّرة bcrypt — مش مقروءة من الـ API) | صف واحد |
| `portfolio_events` | تتبع الزوار (مشاهدات، تحميل CV، ضغطات) | صف لكل حدث |
| `portfolio_messages` | رسائل الفورم + حالتها (new / read / replied) | صف لكل رسالة |

العمود `sort` في كل جدول بيتحكم في **ترتيب الظهور** في الموقع.
العمود `extra` بيحفظ أي حقول إضافية مش معرّفة — **مفيش أي داتا بتضيع أبدًا**.

جدول `portfolio` نفسه بقى **View** بيركّب نفس شكل JSON القديم تلقائيًا من الجداول دي —
عشان كده `index.html` و `dashboard.html` شغالين **بدون أي تغيير**.

---

## 🚀 إزاي تشغّلها (مرة واحدة — دقيقتين)

1. افتح `setup.sql` من المشروع وانسخه كله.
2. Supabase Dashboard → **SQL Editor** → New query → الصق → **Run**.
3. خلاص ✅ — السكريبت بنفسه:
   - بينقل بياناتك الحالية للجداول الجديدة **تلقائيًا**.
   - بيتحقق إن البيانات المنقولة مطابقة للأصل 100% (لو حصل أي اختلاف بيلغي كل حاجة ويقف — مفيش فقدان بيانات ممكن).
   - بيسيب نسخة احتياطية من الشكل القديم في `portfolio_legacy_backup`.
   - بيطبعلك ملخص بعدد الصفوف في كل جدول.

> تشغيل السكريبت أكتر من مرة آمن — بيكتشف إنه شغال قبل كده ومش بيكرر حاجة.

---

## 👀 زوار الموقع (Analytics) — البيانات دي معناها إيه؟

كل شخص يفتح موقعك بيسجل أحداث في جدول `portfolio_events`، وبتشوفها ملخّصة في
**dashboard.html → تبويب 📊 Analytics**:

| الحدث | معناه |
|---|---|
| `view` | زيارة حقيقية للموقع — **بتتحسب مرة واحدة كل 30 دقيقة للزائر** (الـ refresh مش بيزوّد الأرقام) ومعاينة الداشبورد نفسها **مش بتتحسب أبدًا** |
| `project_view` | حدث لما حد يفتح مشروع من مشاريعك (وبيسجل اسم المشروع) |
| `cv_download` | حدث لما حد يحمّل الـ CV بتاعك |
| `github_click` / `linkedin_click` | ضغطات على لينكات GitHub / LinkedIn |
| `contact_submit` | حدث لما حد يبعت رسالة من الفورم (بيتخزن صف كامل في `portfolio_messages` كمان) |
| `location` | سطر نادر بيضيفه الموقع لما **المدينة/الدولة** بتاعت الزائر توصل متأخر بعد أول حدث في الزيارة — بيحقّن الموقع في السجل. **مش بيتحسب زيارة** |

### أسماء المصادر كاملة + أرقام زوار مرتّبة
- عمود المصدر بيظهر **باسم المكان الكامل والمعروف**: `instagram` / `facebook` / `linkedin` / `twitter` / `whatsapp` / `github`... وحتى الاختصارات القديمة زي `ig` بتتحول لـ `instagram` تلقائيًا. أي موقع تاني بيظهر باسمه الكامل (مثل `google.com`).
- عمود `visitor_id` بقى **أرقام قصيرة تسلسلية**: `V-0001, V-0002, V-0003...` — كل متصفح بياخد رقمه من قاعدة البيانات أول زيارة (عن طريق الدالة `next_visitor_id`) وبيحتفظ بيه. الزوار القدام (الـ IDs العشوائية القديمة) بيفضلوا في السجل التاريخي زي ما هما، وأي زيارة جديدة هتاخد رقم جديد مرتّب.

العمود `meta -> source` بيقولك **الزائر جاي منين**: `instagram` أو `facebook`
أو اسم الموقع المحوّل له، أو `direct` (كتب الرابط بنفسه). الموقع بيكتشف ده
تلقائيًا من لينك الـ bio بتاعك (utm_source) أو من رابط الفيسبوك (fbclid).

### 🌍 موقع الزائر: المدينة + الدولة (من الـ IP — **بدون أي إذن موقع**)
من أول ما ينزل التحديث ده، الموقع بيحدّد **المدينة والدولة التقريبية** لكل زائر من
عنوان الـ IP بتاعه، وبيحفظهم مع كل حدث بيعمله.

- **إزاي:** `index.html` بيعمل طلب لخدمة تحديد موقع مجانية عامة من الـ IP.
  بيبدأ بـ `ipwho.is` ولو وقع بيكمل على الترتيب: `ipapi.co` → `freeipapi.com` →
  `api.ip.sb` → `get.geojs.io` → `wtfismyip.com` → `api.my-ip.io`
  (آخر واحد بيرجّع الدولة بس لو كل اللي قبله واقع).
- **لو الـ null ده ظهر عندك في الداشبورد:** فيه تشخيص جاهز — تبويب Analytics →
  **«🔎 Test the location lookup»** بيجرّب الـ 7 خدمات من المتصفح بتاعك واحد واحد
  ويطلعلك ✅/❌ بالسبب، وكمان بيقول كام حدث من آخر 300 حدث فيه مدينة وموعد آخر حدث.
  وعلى الموقع نفسه افتح `https://…/?mmloc` وهيطلعلك بوكس صغير بالنتيجة تحت، مع سطر اختبار اتضاف للجدول.
  الـ null معناه عادة حاجة من اتنين: إن الصف ده اتسجّل **قبل** ما النسخة الجديدة من
  `index.html` تنزل، أو إن المتصفح بيحجب الخدمة (uBlock Origin / Brave Shields /
  Firefox ETP أو VPN) — وفي الحالتين الزيارة نفسها متسجّلة والأرقام صح.
- **مفيش GPS ومفيش نافذة إذن:** الكود مش بيستخدم `navigator.geolocation` نهائيًا —
  المتصفح مش بيسأل الزائر عن أي حاجة، والنتيجة تقريبية على مستوى المدينة (وده المطلوب في التحليلات).
- **إيه اللي بيتخزن:** في `meta` بتاع الحدث: `city` / `region` / `country` / `country_code`.
  **مفيش إحداثيات، ومفيش عنوان الـ IP نفسه** — يعني مفيش أي حاجة دقيقة بتترسّج في الداتابيز.
  مفيش أي تغيير في هيكل الجداول: `meta` هو مكان الحقول الإضافية أصلًا.
- **النتيجة في الداتابيز:** بعد تشغيل `setup.sql` هتلاقي في `portfolio_events`
  3 أعمدة **Generated** جديدة (`city`, `country`, `country_code`) بتتقرا لوحدها من `meta` —
  يعني Table Editor بيعرضها كأي عمود، لكن من غير ما الموقع يبعت أي حاجة مختلفة.
- **النتيجة في الداشبورد:** تبويب 📊 Analytics بقى فيه:
  - كارت **«🌍 Where visitors are»** — زوار كل مدينة (مع علم الدولة) + شريط لكل دولة.
  - قائمة **«Visitors»** — آخر الزوار: رقم الزائر + **المدينة والدولة** + المصدر + عدد الزيارات + آخر ظهور.
  - كارت **🌍 Countries** فوق مع باقي الأرقام.
- **الزوار القدام:** الأحداث المحفوظة قبل التحديث ده مش هيكون ليها مدينة —
  إحنا ما كنانش بنخزن الـ IP، فمفيش إمكانية الرجوع للوراء. أي زيارة جديدة بتتسجّل بالموقع كامل.
- **لو خدمة الـ IP وقعت أو اتأخرت؟** الحدث بيتبعت عادي من غير مدينة — أرقام الزيارات
  **مش بيعتمدوا عليها خالص** — وأول ما الخدمة ترجع المدينة بتتلحق بالسجل
  (سطر `location` واحد بيرجّع المدينة لو وصلت متأخر بعد أول حدث في الزيارة).
- **ولو الداتابيز لسه مش عليها نسخة `setup.sql` الجديدة؟** الداشبورد بيحسب المدن
  والدول بنفسه من الأحداث الخام مباشرة — الكروت شغالة في الحالتين.

### البيانات بتاعت الزوار **جوا Supabase نفسها** — Views جاهزة للقراءة
شغّل `setup.sql` (مرة واحدة) وهتلاقي في **Table Editor** 5 Views جديدة بتلخص الزوار:
- **`site_visitors`** — صف لكل زائر: عدد الزيارات، أول/آخر زيارة، المصدر الأساسي، **المدينة + الدولة**، ميات المشاهدات/التحميلات/الرسايل
- **`site_traffic_daily`** — صف لكل يوم: زوار، زيارات، مشروع اتفتح، CV اتحمل، ضغطات، رسايل
- **`site_events_log`** — كل حدث بمسمى مفهوم (`Opened project: Fire Fighting Car` بدل `project_view`) + المصدر + **الموقع** + التفاصيل
- **`site_traffic_by_country`** — صف لكل دولة: زوار، زيارات، فتحات مشاريع، تحميلات، رسايل
- **`site_traffic_by_city`** — صف لكل مدينة (مع دولتها): زوار، زيارات، آخر ظهور

> 🔒 هتلاقي عليهم علامة `UNRESTRICTED` صفرا/حمرا في Table Editor — دي معناها إن RLS مش مفعّل،
> والـ RLS أصلاً مش بيتفعّل على الـ Views في PostgreSQL. عشان كده السكريبت بيقفلهم بطريقة تانية:
> شال صلاحية القراءة من المفتاح العام — يعني مفيش حد يقدر يقراهم من الـ API غيرك انت من الداشبورد
> (الموقع والداشبورد أصلًا بيقراو من `portfolio_events` و`get_analytics` مش من الـ Views دي).

كمان السكريبت بيضبط السجل القديم تلقائيًا:
- بتقص روابط الـ fbclid/gclid الطويلة من العمود `path`
- بيستنتج مصدر الزيارات القديمة (من utm/fbclid) ويكتبه في `meta.source`
- بيوحّد أسماء الأحداث (`download_cv` القديمة بتبقى `cv_download`)
- بيضيف indexes على السجل عشان الاستعلامات تفضل سريعة
- **تنضيف تلقائي**: أي حدث أقدم من 180 يوم بيتمسح يوميًا 3 صباحًا (عن طريق pg_cron — ولو مش متاح، بيتشال بنجاح من غير ما يبوظ السكريبت)

### ⚠️ لو ظهر خطأ وقت تشغيل `setup.sql`
السكريبت كله بيتشغّل داخل transaction واحدة: أي سطر يفشّل بيُلغي كل حاجة، فالداتابيز
ما بتفضلش نص متحدّثة. بعد التصليح، إعادة تشغيل الملف كامل كفاية — آمن تكراره أي عدد
من المرات وهو بيصلّح الناقص تلقائيًا.

**القاعدة اللي يمنع بيها السكريبت ده الغلط:** أي عمود جديد في `site_visitors` أو
`site_events_log` بيتضاف **في آخر العمودات** — لأن `CREATE OR REPLACE VIEW` بيرفض أي
إعادة ترتيب أو تغيير اسم لعمود موجود (يطلع خطأ
`cannot change name of view column "..."`). عشان كده في `portfolio_events` استخدمنا
أعمدة `GENERATED` (اللي بتقرا من `meta`) بدل ما نغيّر ترتيب أي View.

### إزاي تبدأ أرقام نظيفة من جديد؟ (اختياري)
الأرقام القديمة (قبل 2026-09-19) فيها refreshes متكررة من نفس الزائر — لو حابب
تبدأ من صفحة نظيفة، شغّل ده في SQL Editor:
```sql
truncate table public.portfolio_events;
```

### التبويب مش بيضل فاضي أبدًا
لو دالة `get_analytics` مش موجودة أو فشلت لأي سبب، الداشبورد بيحسب الإحصائيات
من الأحداث مباشرة وبيظهرلك تنبيه صغير — الأرقام بتشتغل في الحالتين.

---

## ✏️ تعدّل منين؟

- **من الداشبورد** (dashboard.html) زي الأول — كل حاجة زي ما هي.
- **أو من Supabase Table Editor مباشرة** — عدّل أي صف (مثلاً غيّر عنوان مشروع) واحفظ؛
  الموقع هيعرض التعديل فورًا. أول ما تفتح الداشبورد بتحمّل آخر حالة فمش هيتنكسر حاجة.

⚠️ الحفظ النهائي بيمر عبر دالة `save_portfolio` المحمية بكلمة مرور الداشبورد،
والجداول نفسها مقفولة على الـ API العام (RLS بدون policies) — القراءة العامة للموقع
بتتم عبر الـ View فقط.

---

## 🔧 إصلاح إضافي تم بالمناسبة

دالة `get_analytics` (تبويب Analytics في الداشبورد) كان فيها غلطة SQL
(`group by 1` مع `count(*)` في نفس التعبير — بترفضها PostgreSQL الحديثة)،
يعني على الأغلب **كانت مش شغالة عندك أصلًا**. النسخة الجديدة بتشغّلها صح.

---

## 🔐 ملاحظات أمان

- `site_*` و `portfolio_auth`: RLS مفعّل **بدون أي policies** → مفيش أي وصول مباشر من الـ API.
- الكتابة بس عبر `save_portfolio` / `change_password` / `update_message` (محمية بكلمة المرور).
- `portfolio_events` / `portfolio_messages`: anyone يستخدم الموقع يقدر يضيف (insert) بس —
  والقراءة مفتوحة عشان الداشبورد (بياناتها غير حساسة).
- الـ anon عنده execute على `build_portfolio_content` بس — دالة read-only بترجع نفس محتوى الـ View العام.
