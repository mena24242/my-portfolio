# 🗄️ خريطة البيانات على Supabase

آخر تحديث: **2026-09-18** — نسخة `setup.sql` v3 (البيانات المنظمة)

---

## المشكلة القديمة

كل محتوى الموقع كان متخزن في **خلية JSON واحدة ضخمة** جوّه جدول `portfolio`.
تفتح Supabase → Table Editor تلاقي سطر واحد مليون حرف — مش مقروء ومش قابل للتعديل.

## الشكل الجديد

المحتوى اتفكك لجداول حقيقية — كل جدول تشوفه وتعدّله مباشرة من
**Supabase Dashboard → Table Editor**:

| الجدول | فيه إيه | شكله |
|---|---|---|
| `site_content` | نصوص الأقسام: الهيرو، نبذة عني، تواصل معي، الفوتر، الإعدادات، التصميم | صف واحد لكل قسم باسم واضح |
| `site_projects` | المشاريع | صف لكل مشروع — أعمدة: العنوان، السنة، التصنيف، الدور، GitHub، الكود، النقاط |
| `site_experience` | الخبرات والأنشطة | صف لكل خبرة (العنوان، الدور، الفترة، التقييم، النقاط) |
| `site_education` | التعليم | صف لكل مرحلة دراسية |
| `site_services` | الخدمات | صف لكل خدمة (الأيقونة، العنوان، الوصف) |
| `site_skills` | مجموعات المهارات | صف لكل مجموعة — المهارات في عمود `chips[]` واضح |
| `site_certifications` | الشهادات | صف لكل شهادة (الكود، الجهة، الوصف، الرابط) |
| `site_testimonials` | التوصيات | صف لكل توصية |
| `site_meta` | وقت آخر حفظ | صف واحد |
| `portfolio_auth` | كلمة مرور الداشبورد (مشفّرة bcrypt — مش مقروءة من الـ API) | صف واحد |
| `portfolio_events` | تتبع الزوار (مشاهدات، تحميل CV، ضغطات) | صف لكل حدث |
| `portfolio_messages` | رسائل الفورم + حالتها (new / read / replied) | صف لكل رسالة |

العمود `sort` في كل جدول بيتحكم في **ترتيب الظهور** في الموقع.
العمود `extra` بيحفظ أي حقول إضافية مش معرّفة — **مفيش أي داتا بتضيع أبدًا**.

جدول `portfolio` نفسه بقى **View** بيركّب نفس شكل JSON القديم تلقائيًا من الجداول دي —
عشان كده `index.html` و `dashboard.html` شغالين **بدون أي تغيير**.

---

## 🚀 إزاي تشغّلها (مرة واحدة — دقيقتين)

1. افتح `setup.sql` من المشروع وانسخه كله.
2. Supabase Dashboard → **SQL Editor** → New query → الصق → **Run**.
3. خلاص ✅ — السكريبت بنفسه:
   - بينقل بياناتك الحالية للجداول الجديدة **تلقائيًا**.
   - بيتحقق إن البيانات المنقولة مطابقة للأصل 100% (لو حصل أي اختلاف بيلغي كل حاجة ويقف — مفيش فقدان بيانات ممكن).
   - بيسيب نسخة احتياطية من الشكل القديم في `portfolio_legacy_backup`.
   - بيطبعلك ملخص بعدد الصفوف في كل جدول.

> تشغيل السكريبت أكتر من مرة آمن — بيكتشف إنه شغال قبل كده ومش بيكرر حاجة.

---

## ✏️ تعدّل منين؟

- **من الداشبورد** (dashboard.html) زي الأول — كل حاجة زي ما هي.
- **أو من Supabase Table Editor مباشرة** — عدّل أي صف (مثلاً غيّر عنوان مشروع) واحفظ؛
  الموقع هيعرض التعديل فورًا. أول ما تفتح الداشبورد بتحمّل آخر حالة فمش هيتنكسر حاجة.

⚠️ الحفظ النهائي بيمر عبر دالة `save_portfolio` المحمية بكلمة مرور الداشبورد،
والجداول نفسها مقفولة على الـ API العام (RLS بدون policies) — القراءة العامة للموقع
بتتم عبر الـ View فقط.

---

## 🔧 إصلاح إضافي تم بالمناسبة

دالة `get_analytics` (تبويب Analytics في الداشبورد) كان فيها غلطة SQL
(`group by 1` مع `count(*)` في نفس التعبير — بترفضها PostgreSQL الحديثة)،
يعني على الأغلب **كانت مش شغالة عندك أصلًا**. النسخة الجديدة بتشغّلها صح.

---

## 🔐 ملاحظات أمان

- `site_*` و `portfolio_auth`: RLS مفعّل **بدون أي policies** → مفيش أي وصول مباشر من الـ API.
- الكتابة بس عبر `save_portfolio` / `change_password` / `update_message` (محمية بكلمة المرور).
- `portfolio_events` / `portfolio_messages`: anyone يستخدم الموقع يقدر يضيف (insert) بس —
  والقراءة مفتوحة عشان الداشبورد (بياناتها غير حساسة).
- الـ anon عنده execute على `build_portfolio_content` بس — دالة read-only بترجع نفس محتوى الـ View العام.
